spring.application.name=finsight_backend

spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}


# JWT Configuration
jwt.secret=your-secret-key-here-make-it-long-and-secure-for-production
jwt.expiration=86400000


finnhub.api.key=d2t4uo1r01qkuv3jaumgd2t4uo1r01qkuv3jaun0

# Resilience4j for Finnhub retries
resilience4j.retry.instances.finnhub.max-attempts=3
resilience4j.retry.instances.finnhub.wait-duration=500ms
resilience4j.retry.instances.finnhub.retry-exceptions=java.io.IOException,org.springframework.web.client.HttpServerErrorException

# Flyway (SQL migrations)
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.repair-on-migrate=true


# Cache (quotes)
spring.cache.type=caffeine
spring.cache.caffeine.spec=expireAfterWrite=15s,maximumSize=10000

simulator.enabled=false

logging.level.com.example.finsight_backend=DEBUG
logging.level.org.springframework.web.socket=DEBUG

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sanixu.hasho@gmail.com
spring.mail.password=irhk ltjd mahj jyav
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL for email links
app.frontend.url=http://localhost:5173

# OAuth2 Configuration
# Set these environment variables or create a .env file with your Google OAuth credentials
# Run setup-oauth.sh (Linux/Mac) or setup-oauth.bat (Windows) to get started

spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google

